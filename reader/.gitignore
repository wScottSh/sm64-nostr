# The reader page's test-only devDependencies (@noble/curves/nostr-tools,
# used ONLY by test/reader.test.mjs as independent oracles -- see that
# file's own header comment). Deliberately not committed/vendored, mirroring
# tools/.gitignore's identical /node_modules/ rule for the same reason.
/node_modules/

# GENERATED by tools/gen_transport_contract_js.py (`npm run generate`) from
# src/pipeline/transport_contract.h/event_id.h/format_descriptor.json --
# never committed, mirroring how tools/pipeline_test/build/include's own
# generated format_descriptor.h/event_profile.h are gitignored build
# outputs, not checked-in source (see the root .gitignore's identical rule
# for that directory). Regenerate before testing (`npm test` already does
# this via its own "pretest" script) or before deploying this page.
/generated/

# Self-signed cert generated on demand by serve-debug.mjs for the phone-facing
# HTTPS spin-up of debug.html -- a local dev artifact, never committed.
/.debug-certs/
