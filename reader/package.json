{
  "name": "sm64-nostr-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "python3 ../tools/gen_transport_contract_js.py --out generated/transport_contract.js",
    "pretest": "npm run generate",
    "build": "npm run generate",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@noble/curves": "^2.4.0",
    "nostr-tools": "^2.25.2"
  }
}
