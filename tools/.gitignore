/aifc_decode
/aiff_extract_codebook
/armips
/extract_data_for_mio
/patch_elf_32bit
/skyconv
/tabledesign
/textconv
/vadpcm_enc
/pipeline_test/pipeline_test
/pipeline_test/pipeline_test.exe
/pipeline_test/gen_reader_fixture
/pipeline_test/gen_reader_fixture.exe
# Ad hoc `npm install` target for the one-off independent-oracle scripts
# (reference_event_id.js, verify_schnorr_reference.js) -- deliberately not
# committed/vendored, since these are one-off oracle runs, not a build
# dependency (see either script's own header comment).
/node_modules/
!/ido5.3_compiler/lib/*.so
!/ido5.3_compiler/usr/lib/*.so
!/ido5.3_compiler/usr/lib/*.so.1
!/ido5.3_compiler/**/*.o
